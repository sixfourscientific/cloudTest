
process {

    withName: 'Data:Command1Software1:.*' {
    
        publishDir = [ 
            enabled    : false,
            mode       : 'copy',
            overwrite  : true,
            path       : params.pubDir,
            saveAs     : { path -> 
                return [
                    'supplementary', CoreMeta.name, 'SOFTWARE1', 'COMMAND1', CoreMeta.ID, CoreMeta.TAG, 
                    new File(path).getName(),
                    ].join('/') }
            ]
    
        }

    ////LEAF_CONFIG////

    }
