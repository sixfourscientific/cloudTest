
profileDir = './profiles'

params { 

    profileLabels = ['local','login','bigmem','aws_batch']
    
    }

includeConfig "$profileDir/basic.config"

includeConfig "$profileDir/local.config"

includeConfig "$profileDir/login.config"

includeConfig "$profileDir/aws_batch.config"
